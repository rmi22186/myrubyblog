<h3><%= @post.title %></h3>
<small><%= @post.created_at.strftime("%b %d, 20%y") %></small>
	<p><%= @post.body %></p>

<%= link_to "Edit this post", edit_post_path %> | <%= link_to "Delete this post", @post, :confirm => "Are you sure you want to delete?", :method => :delete %>